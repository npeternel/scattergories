{
  "name": "scattergories",
  "version": "1.0.0",
  "description": "Online scattergories",
  "engines": {
    "node": "10.x"
  },
  "scripts": {
    "start": "node server/index.js",
    "dev": "nodemon server/index.js",
    "test": "tape server/test/*.test.js",
    "build": "cd react-ui/ && npm install && npm run build"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/npeternel/scattergories.git"
  },
  "dependencies": {
    "express": "^4.17.1",
    "locks": "^0.2.2",
    "socket.io": "^2.3.0",
    "uuid": "^8.1.0"
  },
  "devDependencies": {
    "nodemon": "^2.0.4",
    "tape": "^5.0.0"
  }
}
