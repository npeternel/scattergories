import React from 'react';

const Category = ({ }) => {
  return (
    <div>
      <form>
        <input
          value={}
          onChange={handleAnswerChange}/>
      </form>
    </div>
  )
}

export default Category;